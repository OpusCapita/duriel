'use strict';
const Logger = require('../EpicLogger');
const log = new Logger();
const fs = require('fs');

module.exports = function (config, proxy) {
getExistingUsers(config, proxy)

    proxy.
};


const getExistingUsers = function (config, proxy) {

};