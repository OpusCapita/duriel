
{{ name }}
{% if errors | length %}

Errors:
|{{ padBoth('Library', '.', length) }}|{{ padBoth( 'Expected', '.', length) }}|{{ padBoth('Installed', '.', length) }}|{{ padBoth('Origin', '.', length) }}|{{ padBoth('Reason', '.', length) }}|
{{ padLeft('', '-', length * 5 + 6) }}
{% for error in errors %}
|{{ padLeft(error.library, '.', length) }}|{{ padLeft(error.expected, '.', length) }}|{{ padLeft(error.installed, '.', length) }}|{{ padLeft(error.service, '.', length) }}|{{ padLeft(error.reason, '.', length) }}|
{% endfor %}
{% endif %}
{% if passing | length %}

Passing:
|{{ padBoth('Library', '.', length) }}|{{ padBoth( 'Expected', '.', length) }}|{{ padBoth('Installed', '.', length) }}|{{ padBoth('Origin', '.', length) }}|{{ padBoth('Reason', '.', length) }}|
{{ padLeft('', '-', length * 5 + 6) }}
{% for entry in passing %}
|{{ padLeft(entry.library, '.', length) }}|{{ padLeft(entry.expected, '.', length) }}|{{ padLeft(entry.installed, '.', length) }}|{{ padLeft(entry.service, '.', length) }}|{{ padLeft(entry.reason, '.', length) }}|
{% endfor %}
{% endif %}